
<div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url('https://res.cloudinary.com/louisgadam/image/upload/v1621292532/yael-gonzalez-vdbOJuueLpU-unsplash_uxv8a4.jpg')">
  <div class="banner-container">
    <div class="banner-content">
      <h1><strong>FIGHT CLUB</strong></h1>
      <% if user_signed_in? %>
          <%= link_to 'Go find a fight', locations_path, class: 'btn btn-ghost' %>
        <% else %>
          <%= link_to 'Become a fighter', new_user_registration_path, class: 'btn btn-ghost' %>
        <% end %>
    </div>
  </div>
  <% if user_signed_in? %>
    <%= link_to "Log out", destroy_user_session_path, method: :delete, class: "login-btn btn-ghost" %>
  <% else %>
    <%= link_to 'login', locations_path, class: 'login-btn btn-ghost' %>
  <% end %>
</div>
